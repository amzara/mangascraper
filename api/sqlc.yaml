version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "migrations"
    gen:
      go:
        package: "repository" 
        out: "internal/repository" 
        sql_package: "pgx/v5" 
        emit_db_tags: true 